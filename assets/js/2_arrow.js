/*
    Transform the next function into a one liner arrow function:

    suma = function(a, b){
        return a + b
    }
*/

// a one liner arrow function that returns a+b
// note: one liner arrow functions have a implicit return.
let suma = (a, b) => a + b